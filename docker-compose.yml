services:
  home-monitor:
    image: home-monitor:latest
    container_name: home-monitor
    restart: unless-stopped
    ports:
      - "8088:8088"
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
    env_file:
      - .env
